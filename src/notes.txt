# vim: syntax=yaml
TODO:
-----------------------------------------------------------------
  * Port ninja changes to new version.
    - This should speed things up by 10-15% by putting large TUs
      first. This still requires having the .ninja_log, but
      that's probably not an issue because it doesn't go away on
      ninja clean, so we'd only ever lose it if we delete the
      .builds folder outright, which is rare.
  * Multiplexing:
    - There will be multiple channels to the redis server.
    - Need to attempt multiplexing with ssh -b.
    - Workers should have an id and we should multiplex based
      on bandwidth in each channel.
  * Distributor:
    - Periodically the Distributor will check all the node spe-
      cific queues and grab their pids from the task objects and
      then check the worker advertisement to see if that pid is
      still alive and, if not, it will reclaim the task (just
      putting it back into the global queue). When it does that
      it will increase a counter on the task object so that it
      doesn't do too many retries in case there is something
      wrong with the task itself that is causing the worker to
      die. In the event that the retry count is exceeded then it
      will fail the task by creating the output with a failed
      status code, that way the message will appear to the
      builder. Initially, we can set the retry count at zero so
      that we can get a better sense of worker deaths.
      - Note that this shouldn't be handled by the node manager
        since the node manager goes away if the entire node dies.
    - Each time a compile finishes (maybe on one reference plat-
      form) a sorted set with the compile time can be maintained.
      Then, the distributor can look at that to determine if the
      file is in the top N of heavy TUs and assign it to a desig-
      nated box's host-specific queue. Moreover, it can put it in
      the front of that host's queue so that it gets run ASAP.
      That way we ensure that a slow box never gets stuck han-
      dling a heavy TU.
    - Alternatively, we could just have a tool that generates
      this sorted set from the contents of the ninja log file.
      But that would be an extra manual step; would be nice to
      have it be automated.
  * Try doing more to split up the large TUs. Run
    tools/ninja-log-times.sh to see which.
  * Need to go through and minimize the number of redis queries
    made by the builder and workers because latency can be very
    high when operating externally. For example, one query can
    have a latency (round trip) of 1/2 second.
    - Consider putting any multi-step things into server-side lua
      functions.
  * Dashboard:
    * The dashboard should go to sleep (increase poll delay after
      a certain amount of time of no utilization), then as soon
      as something happens it should go back to frequent updates.
    * The dashboard can't scale to many more vertical nodes, and
      can't even show all nodes when the full view is enabled. So
      we need to add a new view mode where only the interactive
      widgets are shown, and also have it skip the larger layouts
      when screen rows are not large enough.
  * Node Manager:
    * Should keep track of the number of unexpected worker
      deaths.
    * Since it is going to be long-running it should probably
      have a way to throttle itself to essentially go to sleep
      when not being used.
    * The pool needs to have these settings:
      - Delay before bringing back a failed process.
      - Max failed processes before stopping.
    * It seems that it is still possible for the node manager to
      be terminated (e.g. if it is invoked over an ssh connection
      and the connection somehow drops) but the workers continue
      running. Should figure out why this happens first if possi-
      ble. Then if it can't be fixed in a simple way then we
      should consider putting some logic into the worker to mon-
      itor the presence of its parent process and to stop if it
      goes away. Actually this should be done for any process
      running under the process pool.
  * Worker Death:
    * Deal with timeouts/failures, i.e. when a task never com-
      pletes. This can happen when e.g. a worker that removes the
      task from the queue dies before finishing it. The builder
      should monitor the worker that is working on the task (when
      a worker starts working on a task they should mark that
      task with their identity) and if the worker disappears then
      the task needs to be re-queued.
    * There is currently a cleanup function that will unadvertise
      the worker if it exits on error, though even if it termi-
      nates suddenly, the advertisement should expire within one
      minute.
    * Currently there is a timeout for a running command; make
      sure that is adjusted properly and make sure that the
      worker doesn't abort when a popen times out.
    * A worker should have a flag where it can be given a parent
      pid and if it is given it then it should detect if that pid
      goes away and if so it should stop. This should be done for
      any process run in the process pool.
  * For the local workers, when they are uploading their pre-
    processed output, perhaps we want to adapt the compression
    level according to the upload bandwidth they have to the
    redis server, if that can be measured reliably.
  * Need to set TTLs properly on all the keys.
    - When pushing to the queue log, set the expiry on the list.
  * Move the subprocess code into moon.
  * Have the worker update a sorted set with the time spent so
    far on a translation unit so that the dashboard can query the
    top n of them and show them along with their times and their
    hosts, almost giving a ninja-like output.
  * We may want to reconsider caching the task output (i.e.
    stderr) of a compile command when it fails... because then if
    it fails e.g. due to some lua issue in redist then that will
    be cached.
  * After writing a file from the contents of a blob, stat the
    file size to make sure it is the same as the key.
  * C-c seems to take a second or two when blocked on a socket...
    see if that can be improved.


Plan for Cluster Manager:
-----------------------------------------------------------------
  * Jobs run by Cluster Manager:
    - Redis Database (?) - not sure yet.
    - Distributor
    - Power Meter: will determine the power of each node and up-
      load it as a sorted set to Redis for consumption by the
      Distributor.
    - Node Controller (once we have node managers running under
      systemd). This will bring up/down node managers.
  * There will be a single job, called the cluster manager. The
    cluster manager can run on any host, but probably best to
    have it run on the host with the redis server.
  * It will essentially just run a set of process pools like the
    node manager.
  * The first process it will run will be the Distributor (see
    below).
  * Eventual goals:
    - It should be able to bring up/down node managers on each
      node through remote systemd through ssh.